// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCpaUfH0dxpjyNl_P9sBk0HSuMKzoqi5kw",
    authDomain: "reader-board.firebaseapp.com",
    databaseURL: "https://reader-board.firebaseio.com",
    projectId: "reader-board",
    storageBucket: "reader-board.appspot.com",
    messagingSenderId: "1004430078093",
    appId: "1:1004430078093:web:8e22000208d36b57d3f2c4"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
